<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/action_padding_top"
    android:paddingBottom="@dimen/action_padding_bottom"
    android:paddingStart="@dimen/action_padding_start_end"
    android:paddingEnd="@dimen/action_padding_start_end"
    android:gravity="center_horizontal">
    <ImageButton
        android:id="@+id/action_icon"
        android:layout_width="@dimen/circle_size"
        android:layout_height="@dimen/circle_size"
        android:scaleType="fitCenter"
        android:background="@drawable/action_button"/>
    <TextView
        android:id="@+id/action_label"
        android:gravity="center"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        tools:text="Open in phone"/>

</LinearLayout>